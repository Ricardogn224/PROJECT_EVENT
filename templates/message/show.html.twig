{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.message }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ message.date ? message.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Service</th>
                <td>{{ message.demande }}</td>
            </tr>
            <tr>
                <th>Emetteur</th>
                <td>{{ message.emmeteur }}</td>
            </tr>
            <tr>
                <th>Destinataire</th>
                <td>{{message.recepteur }}</td>
            </tr>
        </tbody>
       
    </table>

    <a href="{{ path('app_message_index') }}">Retour</a>

    <a href="{{ path('app_message_edit', {'id': message.id}) }}">Modifier</a>
    <a href="{{ path('app_message_reply', {'id': message.id}) }}">Répondre</a>

    {{ include('message/_delete_form.html.twig') }}
{% endblock %}
