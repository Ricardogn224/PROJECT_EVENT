{% extends 'base.html.twig' %}

{% block title %}Favori{% endblock %}

{% block body %}
    <h1>Favori</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favori.id }}</td>
            </tr>
            <tr>
                <th>Service_id</th>
                <td>{{ favori.serviceId }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ favori.userId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_favori_index') }}">back to list</a>

    <a href="{{ path('app_favori_edit', {'id': favori.id}) }}">edit</a>

    {{ include('favori/_delete_form.html.twig') }}
{% endblock %}
