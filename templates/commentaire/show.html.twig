{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire.commentaire }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ commentaire.note }}</td>
            </tr>
            <tr>
                <th>Service_id</th>
                <td>{{ commentaire.serviceId }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ commentaire.userId }}</td>
            </tr>
        </tbody>
    </table>

    <a class="text-danger" href="{{ path('app_commentaire_index') }}">Retourner</a>

    <a class="btn-default" href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">Modifier</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
