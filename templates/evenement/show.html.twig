{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h2>Evenements</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ evenement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <a class="text-danger" href="{{ path('app_evenement_index') }}">Retourner</a>
        <a class="btn-default" href="{{ path('app_evenement_edit', {'id': evenement.id}) }}">Modifier</a>
    </div>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
